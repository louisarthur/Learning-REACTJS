module.exports = {
  // presets adicionados pelo yarn e que utilizarei, o primeiro ele altera as funcionalidades do JS que o
  //navegador não entende.
  // o segundo vai transformar as coisas que o navegador não entende na parte do react
  presets: ["@babel/preset-env", "@babel/preset-react"]
};
